# Git
.git
.gitignore
.gitattributes

# Build artifacts
bin/
dist/
*.out
*.test
coverage.txt
coverage.html
*.prof

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Docker files (we don't need these in the build context)
Dockerfile
Dockerfile.dev
.dockerignore
docker-compose.yml

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# Documentation (not needed in container)
*.md
docs/
LICENSE
CHANGELOG.md

# Test data (might be large)
testdata/
tests/

# Local config files (user's runtime configs)
# Note: config.example*.yml and actions.example*.yml are NOT excluded
# as they are needed by Dockerfile
config.yml
actions.yml
config.local.yml
actions.local.yml
*.local.*

# Dependencies (handled by go mod)
vendor/

# Temporary files
tmp/
temp/
*.tmp
*.bak

# Logs
*.log
logs/

# OS specific
Thumbs.db
.Trashes
