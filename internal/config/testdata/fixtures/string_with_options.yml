callable:
  test_action:
    name: "Test Action with String Type Having Options"
    type: http
    parameter_definitions:
      - name: environment
        type: string  # Invalid - string type cannot have options
        required: false
        options: ["dev", "staging", "production"]
        description: "Target environment"
    http:
      url: "https://example.com/webhook"
      method: POST
    timeout: 30
